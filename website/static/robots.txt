User-agent: *
Allow: /

# /en/ is a Hugo-generated meta-refresh alias for the English homepage
# (defaultContentLanguageInSubdir=false puts EN at root; /en/ is just
# an alias stub). Keep it out of the index — but let the per-language
# sitemap through, since the root sitemap index references it.
Disallow: /en/
Allow: /en/sitemap.xml

Sitemap: https://highlandcloudforest.com/sitemap.xml
